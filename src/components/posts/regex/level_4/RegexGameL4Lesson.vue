<template>
<div>
    <h4>Lesson</h4>

    <h5>Matching the metacharacters themselves</h5>

    <p>
        Metacharacters are special characters that help with our searches in regex. But what if we have to search for the metacharacter itself. In that case, a backslash <code>\</code> comes to rescue. <code>\.</code> will search for literal fullstop <code>.</code> or <code>\[</code> will search for literal left square bracket <code>[</code>
    </p>

    <p>
        using fullstop with charcter sets results in an expression which searches for literal full stop as well.<br> <code>[.]</code> searches for <code>.</code>
    </p>
</div>
</template>
